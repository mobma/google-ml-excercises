# binary-classification-rice
# objectives:
- training a binary classifier
- calculating different metrics at different thresholds for a binary classifier
- comparing ROC and AUC for different models
  
